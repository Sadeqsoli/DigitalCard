# Shared UI package instructions

These instructions apply to `packages/ui`. Also follow the root and mobile instructions where relevant.

- Components are presentation-only.
- Do not make API calls or access a database.
- Do not implement authentication, authorization, or other domain/business rules.
- Support iOS, Android, and Web unless an exception is explicitly documented.
- Use shared design tokens when the design system is introduced.
